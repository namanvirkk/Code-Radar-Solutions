#include <stdio.h>

int main() {
    int Hexadecimal;
    printf("Hexadecimal: %s\n", Hexadecimal );
    scanf("%s", &Hexadecimal);

    int Octal;
    scanf("%d", &Octal);
    printf("Octal: %d", Octal );

    return 0;
}