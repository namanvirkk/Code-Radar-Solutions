#include <stdio.h>

int main() {
    char Hexadecimal;
    // Read hexadecimal input using %x
    scanf("%c", &Hexadecimal);
    printf("Hexadecimal: %c\n", Hexadecimal);

    int a;
    scanf("%d", &a);
    printf("Octal: %d", a);

    return 0;
}
