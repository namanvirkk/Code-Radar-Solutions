#include <stdio.h>

int main() {
    int Hexadecimal;
    scanf("%d", &Hexadecimal);

    int Octal;
    scanf("%d", &Octal);

    printf("Hexadecimal: %d", Hexadecimal \n);
    printf("Octal: %d", Octal );
    return 0;
}