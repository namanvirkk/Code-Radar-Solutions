#include <stdio.h>

int main() {
    int Hexadecimal;
    printf("Hexadecimal: %s\n", Hexadecimal );
    scanf("%d", &Hexadecimal);

    int Octal;
    scanf("%d", &Octal);
    printf("Octal: %d", Octal );

    return 0;
}