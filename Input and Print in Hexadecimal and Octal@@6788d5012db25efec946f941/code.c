#include <stdio.h>

int main() {
    int Hexadecimal;
    printf("Hexadecimal: ",  );
    scanf("%x", &Hexadecimal);

    int Octal;
    scanf("%d", &Octal);
    printf("Octal: %d", Octal );

    return 0;
}