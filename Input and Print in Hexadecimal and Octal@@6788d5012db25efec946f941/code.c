#include <stdio.h>

int main() {
    int Hexadecimal;
    // Read hexadecimal input using %x
    scanf("%x", &Hexadecimal);
    printf("Hexadecimal: %x\n", Hexadecimal);

    int a;
    scanf("%d", &a);
    printf("Octal: %d", a);

    return 0;
}
