#include <stdio.h>
int main(){
    int a;
    scanf("%d", &a);
    if ( a % 4 ==0 && (a %100 !== 0|| (a% 400 == 0))){
        printf("%d", a);
    }
    else if( b > a){
        printf("%d", b);
    }
    else{
        printf("0");
    }
    return 0;
}
    
    